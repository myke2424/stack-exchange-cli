[tool.poetry]
name = "stack-exchange-cli"
version = "0.1.0"
description = "Stack Exchange CLI "
authors = ["Mike Davidson"]

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.28.0"
rich = "^12.4.4"
toml = "^0.10.2"
redis = "^4.3.4"

[tool.poetry.dev-dependencies]
pytest = "^7.1.2"
black = "^22.3.0"
yapf = "^0.32.0"
flake8 = "^4.0.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
